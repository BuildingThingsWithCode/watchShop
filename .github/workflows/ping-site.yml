name: Ping Website Every 5 Minutes

on:
  schedule:
    - cron: '5-59/5 8-16 * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping the website
        run: |
          curl -I -m 30 -L https://defensive-glori-larsmartens-4c10bcc4.koyeb.app/
